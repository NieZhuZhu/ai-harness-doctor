name: "AI Harness Doctor"
description: "Audit and drift-guard AGENTS.md and AI agent configs for stale commands/paths, conflicts, and security risks; emit SARIF."
author: "NieZhuZhu"
branding:
  icon: "check-circle"
  color: "purple"
inputs:
  path:
    description: "Repository path to scan."
    required: false
    default: "."
  command:
    description: "Subcommand to run (scan or drift)."
    required: false
    default: "scan"
  args:
    description: "Extra arguments passed through to the command."
    required: false
    default: ""
  sarif-file:
    description: "Path to write the SARIF output file."
    required: false
    default: "ai-harness-doctor.sarif"
  version:
    description: "Implementation to run: bundled (selected Action ref) or an npm version/tag."
    required: false
    default: "bundled"
outputs:
  sarif-file:
    description: "Path to the generated SARIF file."
    value: ${{ steps.run.outputs.sarif-file }}
runs:
  using: composite
  steps:
    - id: run
      shell: bash
      env:
        INPUT_ARGS: ${{ inputs.args }}
        INPUT_COMMAND: ${{ inputs.command }}
        INPUT_PATH: ${{ inputs.path }}
        INPUT_SARIF_FILE: ${{ inputs.sarif-file }}
        INPUT_VERSION: ${{ inputs.version }}
        ACTION_PATH: ${{ github.action_path }}
      run: |
        set -euo pipefail

        case "$INPUT_COMMAND" in
          scan|drift) ;;
          *)
            echo "Unsupported command: $INPUT_COMMAND (expected scan or drift)" >&2
            exit 2
            ;;
        esac

        if [ "$INPUT_VERSION" = "bundled" ]; then
          cli="$ACTION_PATH/bin/cli.js"
        else
          install_dir="$RUNNER_TEMP/ai-harness-doctor-action"
          rm -rf "$install_dir"
          mkdir -p "$install_dir"
          npm install \
            --prefix "$install_dir" \
            --no-audit \
            --no-fund \
            --ignore-scripts \
            "ai-harness-doctor@$INPUT_VERSION"
          cli="$install_dir/node_modules/ai-harness-doctor/bin/cli.js"
        fi

        run_args=("$INPUT_COMMAND" "$INPUT_PATH" "--sarif")
        if [ -n "$INPUT_ARGS" ]; then
          read -r -a extra_args <<< "$INPUT_ARGS"
          run_args+=("${extra_args[@]}")
        fi
        node "$cli" "${run_args[@]}" > "$INPUT_SARIF_FILE"
        echo "sarif-file=$INPUT_SARIF_FILE" >> "$GITHUB_OUTPUT"
